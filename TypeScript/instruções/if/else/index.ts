const max = 100
let counter = 100


// ==> if
if (counter < max) {
    counter++
}

// ==> if else
if (counter < max) {
    counter++
} else {
    console.log('cálculo finalizado!')
}

// ==> Ternário
const ativo = true ? false : true