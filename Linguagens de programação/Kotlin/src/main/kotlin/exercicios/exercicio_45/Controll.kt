package exercicios.exercicio_45

class Port {
    fun controll() {
        val age = Console.readInt("Qual sua idade?")

        if(age < 18) return println("Negado. Menores não são permitidos")

        val invitationType = Console.readString("Qual é o tipo de convite")

        if(invitationType < ) return println("Negado. Menores não são permitidos")
    }
}